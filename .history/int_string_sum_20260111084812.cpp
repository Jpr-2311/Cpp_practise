#include <bits/stdc++.h>
using namespace std;

class solution{
    public:
    void operate( vector<pair<int, string>> &pairs) {
        int sum=0;
        string result="";
        for(auto p : pairs){
            sum += p.first; 
            result+=p.second;
        }
        cout << sum<<endl;
        cout << result<<endl;
        cout<< result.length() << endl;
        #include <bits/stdc++.h>
using namespace std;

class solution{
    public:
    void operate( vector<pair<int, string>> &pairs) {
        int sum=0;
        string result="";
        for(auto p : pairs){
            sum += p.first; 
            result+=p.second;
        }
        cout << sum<<endl;
        cout << result<<endl;
        cout<< result.length() << endl;
        
        
    }
};

        
    }
};
